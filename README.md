# com.dotcms.rest.page
OSGi REST endpoint to get a page, its template and containers in a single call

It provides two methods:

/api/page/json/{pathToYourPage}

and 

/api/page/snippets/{pathToYourPage}
